source "https://rubygems.org"

# Root Gemfile for the lakeraven-integrations monorepo.
# Each gemspec subdirectory has its own .gemspec; this Gemfile is only for
# monorepo-level tooling (rake, rubocop, etc.).

gem "rake", "~> 13.0"
gem "minitest", "~> 5.0"
gem "rubocop-rails-omakase", require: false
